# Raw Claude.ai chat export — contains personal back-and-forth and secrets.
# Never commit. Source of truth for the guide is HomeNAS_Setup_Guide.md.
HomeNAS_Setup.md

# Original Pages source — superseded by HomeNAS_Setup_Guide.md.
HomeNAS_Setup_Guide.pages

# macOS junk
.DS_Store
._*

# Editor backups
*.swp
*.swo
*~

# Pandoc build artifact — PDF is generated from Markdown via `make pdf`.
# Source of truth is HomeNAS_Setup_Guide.md.
HomeNAS_Setup_Guide.pdf

# Buyer-facing bundle — built via `make zip`.
HomeNAS_Setup_Guide.zip
